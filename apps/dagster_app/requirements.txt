# Dagster core
dagster==1.11.13
dagster-webserver==1.11.13
# dagster-postgres uses 0.x versioning, not 1.x like dagster core
dagster-postgres~=0.27.0

# Cloud integrations
# Use compatible version range to avoid dependency conflicts
dagster-aws~=0.27.0

# HTTP client for API calls
requests==2.32.3

# Data processing (if needed in future)
pandas==2.2.3
